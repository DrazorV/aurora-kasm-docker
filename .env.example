# Absolute host path containing the shared Aurora data.
AURORA_DATA_PATH=/srv/aurora

# Bind address and host port exposed to the reverse proxy.
AURORA_BIND_IP=0.0.0.0
AURORA_PORT=8444

# Runtime locale and timezone.
TZ=Europe/Athens
